# Story 9.5: Backend Persist Audit Events for Historical Replay

## Status
Draft

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["Read", "Grep"]
```

## Story
**As a** user,
**I want** to review the audit trail for past research runs (not just the current one),
**so that** I can go back and examine what queries were generated, which sources were visited, and how scores were computed for any historical job.

## Acceptance Criteria
1. [ ] New `audit_events` SQLite table: `(id INTEGER PRIMARY KEY AUTOINCREMENT, job_id TEXT NOT NULL, event_type TEXT NOT NULL, payload_json TEXT NOT NULL, created_at TEXT NOT NULL)`.
2. [ ] Index on `job_id` for efficient querying.
3. [ ] `save_audit_event()` method in `repository.py` called alongside each `emit_*` in `progress.py`.
4. [ ] New endpoint: `GET /api/research/{job_id}/audit` returns events chronologically.
5. [ ] Frontend `<AuditView>` fetches audit events when viewing historical jobs via "View Results".
6. [ ] ~200 events/run, ~40KB — lightweight storage overhead.

## Tasks / Subtasks
- [ ] Add `audit_events` table to SQLite schema in `repository.py`
  - [ ] `id INTEGER PRIMARY KEY AUTOINCREMENT`
  - [ ] `job_id TEXT NOT NULL`
  - [ ] `event_type TEXT NOT NULL`
  - [ ] `payload_json TEXT NOT NULL`
  - [ ] `created_at TEXT NOT NULL`
  - [ ] Create index on `job_id`
- [ ] Implement `save_audit_event(job_id, event_type, payload)` method in `Repository`
- [ ] Call `save_audit_event()` from each `emit_*` function in `progress.py`
  - [ ] Requires passing `job_id` and `Repository` instance (or DB path) to emit functions
- [ ] Add `GET /api/research/{job_id}/audit` endpoint in `router.py`
  - [ ] Returns `List[AuditEvent]` sorted by `created_at`
  - [ ] Returns 404 if job not found
  - [ ] Returns empty list if no audit events
- [ ] Implement `get_audit_events(job_id)` method in `Repository`
- [ ] Update frontend `AuditView` to fetch audit events for historical jobs
  - [ ] Call `/api/research/{job_id}/audit` when user clicks "View Results"
  - [ ] Populate tabs with fetched audit data
  - [ ] Show loading state during fetch
- [ ] Write unit tests
  - [ ] Test `save_audit_event()` persists correctly
  - [ ] Test `get_audit_events()` returns chronological order
  - [ ] Test API endpoint returns events for valid job
  - [ ] Test API endpoint returns 404 for invalid job

## Dev Notes
**Depends on**: Stories 8.1, 9.4.

**Schema creation** — add to existing `_ensure_tables()` method:
```python
await db.execute("""
    CREATE TABLE IF NOT EXISTS audit_events (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        job_id TEXT NOT NULL,
        event_type TEXT NOT NULL,
        payload_json TEXT NOT NULL,
        created_at TEXT NOT NULL
    )
""")
await db.execute("CREATE INDEX IF NOT EXISTS idx_audit_job_id ON audit_events(job_id)")
```

**Progress.py integration challenge**: The current `emit_*` functions only access the state's progress queue. To persist events, they need either:
1. Access to the Repository instance via state, or
2. A separate persistence callback in state

**Recommended approach**: Add `audit_callback` to state that wraps `save_audit_event()`.

**Storage estimate**: ~200 events/run x ~200 bytes/event = ~40KB/run. Very lightweight.

## File List
- `vendor-research-tool-backend/app/repository.py` (modified — new table, new methods)
- `vendor-research-tool-backend/app/graph/progress.py` (modified — persist alongside emit)
- `vendor-research-tool-backend/app/api/router.py` (modified — new endpoint)
- `vendor-research-tool-backend/tests/test_repository.py` (modified — new tests)
- `vendor-research-tool-frontend/src/components/organisms/AuditView.tsx` (modified — fetch historical data)

## Testing
- Location: `vendor-research-tool-backend/tests/test_repository.py`
- New tests for save/get audit events
- API endpoint test for `GET /api/research/{job_id}/audit`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0 | Story created by @sm from PM handoff | River (@sm) |
