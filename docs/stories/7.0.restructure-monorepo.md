# Story 7.0: Restructure Monorepo into Backend + Frontend Directories

## Status
Done

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["Read", "Grep"]
```

## Story
**As a** developer,
**I want** the codebase split into backend and frontend directories,
**so that** each can be deployed independently and the architecture supports a modern React frontend.

## Acceptance Criteria
1. [x] All Python code moved to `vendor-research-tool-backend/` (app/, tests/, requirements.txt, pyproject.toml, .env.example, Makefile, run.sh).
2. [x] `static/index.html` removed (archived in git history).
3. [x] Static file serving removed from `app/main.py` (StaticFiles mount, root() route deleted).
4. [x] CORS configured via `ALLOWED_ORIGINS` env var (default: `http://localhost:5173,http://127.0.0.1:5173`).
5. [x] All backend tests pass after restructure (192 passed, 94% coverage).
6. [x] `.gitignore` updated for frontend paths (.env.development, node_modules, dist).
7. [x] `docs/`, `.claude/`, `.aios-core/` preserved at repo root (shared).

## Tasks / Subtasks
- [x] Move `app/` to `vendor-research-tool-backend/app/`
- [x] Move `tests/` to `vendor-research-tool-backend/tests/`
- [x] Move config files (requirements.txt, pyproject.toml, .env.example, Makefile, run.sh)
- [x] Delete `static/` directory
- [x] Remove StaticFiles mount and root() route from `app/main.py`
- [x] Add `ALLOWED_ORIGINS` env var to CORS configuration
- [x] Update `.gitignore` for frontend paths
- [x] Run `pytest` to verify all tests pass

## Dev Notes
**Depends on**: None (foundation story).

**Key decision**: Backend `app/` moves as a unit so all internal imports remain unchanged. No Python import paths need updating.

**CORS setup**:
```python
ALLOWED_ORIGINS = os.environ.get(
    "ALLOWED_ORIGINS", "http://localhost:5173,http://127.0.0.1:5173"
).split(",")
```

## File List
- `.gitignore` (modified)
- `vendor-research-tool-backend/` (all files moved from root)
- `static/index.html` (deleted)

## Testing
- Location: `vendor-research-tool-backend/tests/`
- All 192 existing tests pass after restructure
- 94% code coverage maintained

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0 | Story created retroactively from implementation | River (@sm) |
