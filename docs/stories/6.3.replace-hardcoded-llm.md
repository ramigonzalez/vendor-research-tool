# Story 6.3: Replace Hardcoded LLM Instantiations

## Status
Done

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["Read", "Grep", "Bash"]
```

## Story
**As a** research pipeline,
**I want** all LLM instantiation sites to use `get_llm()`,
**so that** the provider switch takes effect across the entire pipeline.

## Acceptance Criteria
1. Remove `from langchain_anthropic import ChatAnthropic` from `app/graph/nodes.py`.
2. Add `from app.config import get_llm` and `from langchain_core.language_models import BaseChatModel` to `app/graph/nodes.py`.
3. Replace 4 `ChatAnthropic(model="claude-sonnet-4-5", temperature=0)` calls with `get_llm()`:
   - `generate_queries()` (line ~77)
   - `extract_evidence()` (line ~241)
   - `assess_capabilities()` (line ~447)
   - `generate_summary()` (line ~558)
4. Update 3 helper function type hints from `ChatAnthropic` to `BaseChatModel`:
   - `_generate_query_pair()`
   - `_extract_evidence_for_pair()`
   - `_assess_single_pair()`
5. Update 20 `patch("app.graph.nodes.ChatAnthropic", ...)` in `tests/test_nodes.py` to `patch("app.graph.nodes.get_llm", ...)`.
6. All existing tests pass. Lint and type checks pass.

## Tasks / Subtasks
- [x] Replace `from langchain_anthropic import ChatAnthropic` with new imports
- [x] Replace 4 `ChatAnthropic(...)` calls with `get_llm()`
- [x] Update 3 type hints from `ChatAnthropic` to `BaseChatModel`
- [x] Update all patch targets in `tests/test_nodes.py`
- [x] Verify all 184 tests pass
- [x] Verify ruff lint passes on changed files
- [x] Verify pyright passes with 0 errors

## Dev Notes
**Depends on**: Story 6.2 (`get_llm()` factory)
**Tech Stack**: Python, langchain-core

**Key implementation details**:
- The old pattern `patch("app.graph.nodes.ChatAnthropic", return_value=mock_llm)` patched the class constructor — calling `ChatAnthropic(...)` returned `mock_llm`
- The new pattern `patch("app.graph.nodes.get_llm", return_value=mock_llm)` patches the function — calling `get_llm()` returns `mock_llm`
- Both patterns result in `mock_llm` being the LLM instance, so test assertions are identical
- The `generate_summary` node called `ChatAnthropic(...)` inside a try/except block — now calls `get_llm()` in the same location

### Testing
- Location: `tests/test_nodes.py`
- Framework: `pytest` + `pytest-asyncio`
- 20 patch targets updated (search-and-replace of `ChatAnthropic` to `get_llm`)
- All 184 tests pass after change

### Files Changed
- `app/graph/nodes.py` — Import swap, 4 instantiation replacements, 3 type hint changes
- `tests/test_nodes.py` — 20 patch target updates

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-24 | 1.0 | Initial story creation | Morgan (@pm) |
| 2026-02-24 | 1.1 | Implementation complete | @dev |
