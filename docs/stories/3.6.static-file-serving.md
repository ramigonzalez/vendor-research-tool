# Story 3.6: Static File Serving & App Entry Point

## Status
Draft

## Priority
Must Have — this story initializes the FastAPI app entry point and calls `create_db_and_tables()` on startup. No other story can be run end-to-end without it. (Previously labeled "Could Have" in epic-3; corrected per REC-1.)

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["Read", "Bash"]
```

## Story
**As a** developer,
**I want** FastAPI to serve `static/index.html` at the root URL,
**so that** the frontend is accessible at `http://localhost:8000` without a separate web server.

## Acceptance Criteria
1. `GET /` returns `static/index.html` as HTML response.
2. `StaticFiles` mounted at `/static` serving the `static/` directory.
3. `GET /health` returns `{"status": "ok"}` for health checks.
4. FastAPI lifespan event calls `create_db_and_tables()` on startup.
5. Running `uvicorn app.main:app --reload` from project root starts without errors.
6. `static/` directory created with a placeholder `index.html` (even if frontend not yet built).

## Tasks / Subtasks
- [ ] Wire `StaticFiles` mount in `app/main.py`
- [ ] Add `GET /` route that returns `FileResponse("static/index.html")`
- [ ] Add `GET /health` route
- [ ] Create `static/` directory with placeholder `index.html`
- [ ] Verify `uvicorn app.main:app` starts cleanly

## Dev Notes
```python
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse

app.mount("/static", StaticFiles(directory="static"), name="static")

@app.get("/")
async def root():
    return FileResponse("static/index.html")

@app.get("/health")
async def health():
    return {"status": "ok"}
```

`python-multipart` may be needed: add to requirements.txt if FastAPI warns.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-23 | 1.0 | Initial story creation | Morgan (@pm) |
