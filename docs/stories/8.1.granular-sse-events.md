# Story 8.1: Backend Granular SSE Event Emission

## Status
Done

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["Read", "Grep"]
```

## Story
**As a** frontend developer,
**I want** granular SSE events emitted from the research pipeline,
**so that** I can show real-time feedback for each stage including queries, sources, evidence, scores, and warnings.

## Acceptance Criteria
1. [x] New SSE event types: `phase_start`, `phase_end`, `query_generated`, `search_result`, `evidence_extracted`, `score_computed`, `vendor_ranked`, `warning`, `iteration_start`.
2. [x] Progress percentage never goes backwards (monotonic progress fix).
3. [x] Gap-filling iteration uses 45-50% range instead of resetting to 10%.
4. [x] Extraction failures emit `warning` events (not silently swallowed).
5. [x] Backward-compatible: existing `progress` events preserved.
6. [x] All backend tests pass (192 passed, 94% coverage).

## Tasks / Subtasks
- [x] Create `_emit()` helper in `progress.py` to reduce duplication
- [x] Add `emit_phase_start()` and `emit_phase_end()` functions
- [x] Add `emit_query_generated()` function
- [x] Add `emit_search_result()` function
- [x] Add `emit_evidence_extracted()` function
- [x] Add `emit_score_computed()` function
- [x] Add `emit_vendor_ranked()` function
- [x] Add `emit_warning()` function
- [x] Add `emit_iteration_start()` function
- [x] Fix monotonic progress in `nodes.py` (gap-fill range 45-50%)
- [x] Add `state` parameter to `_extract_evidence_for_pair` for warning emission
- [x] Instrument each pipeline node with appropriate event emissions
- [x] Update test_nodes.py for new `state` parameter

## Dev Notes
**Depends on**: Story 7.0.

**Monotonic progress fix**:
```python
if is_gap_fill:
    pct_start = 45
    pct_range = 5  # Gap-fill gets a smaller range (45-50%)
else:
    pct_start = 10
    pct_range = 35  # Primary search gets 10-45%
```

## File List
- `vendor-research-tool-backend/app/graph/progress.py` (modified — 9 new emit functions)
- `vendor-research-tool-backend/app/graph/nodes.py` (modified — instrumented all nodes)
- `vendor-research-tool-backend/tests/test_nodes.py` (modified — updated for state parameter)

## Testing
- Location: `vendor-research-tool-backend/tests/`
- 192 tests pass, 94% coverage

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0 | Story created retroactively from implementation | River (@sm) |
