# Story 8.4: LiveCounters Component

## Status
Done

## Executor Assignment
```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["Read"]
```

## Story
**As a** user running a research job,
**I want** to see how many sources were found and how long the research has been running,
**so that** I have a quick numerical summary of progress.

## Acceptance Criteria
1. [x] Sources found counter (increments on `search_result` events).
2. [x] Elapsed time MM:SS (useTimer hook with `setInterval`).
3. [x] `aria-atomic="true"` for screen readers.
4. [x] Freezes on completion.

## Tasks / Subtasks
- [x] Create `LiveCounters.tsx` molecule
- [x] Display source count from `useResearchState`
- [x] Display formatted elapsed time
- [x] Add `aria-atomic` and `aria-live` attributes
- [x] Freeze values when `isComplete` is true

## Dev Notes
**Depends on**: Stories 8.2, 8.3.

## File List
- `src/components/molecules/LiveCounters.tsx` (new)

## Testing
- Build verification: `npm run build` passes

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0 | Story created retroactively from implementation | River (@sm) |
