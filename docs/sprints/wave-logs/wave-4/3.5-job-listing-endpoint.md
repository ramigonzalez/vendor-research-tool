# Wave Log: Story 3.5 — GET /api/jobs Listing

## Status: Complete

## Changes Made

### 1. `app/api/router.py` — Appended new endpoint
- Added `GET /api/jobs` endpoint that calls `repo.list_jobs(limit=50)`
- Returns a JSON array of serialized `ResearchJob` objects, most recent first
- Follows existing patterns (Depends injection, `model_dump(mode="json")`)

### 2. `tests/test_api.py` — Appended 3 new tests
- `test_empty_repo_returns_empty_list` — verifies `[]` on clean mock repo
- `test_jobs_returned_when_repo_has_jobs` — adds 2 jobs, verifies both returned
- `test_response_is_json_array` — confirms response is a JSON array

## Verification
- `make check` passes (lint, format-check, typecheck, 148 tests, 93% coverage)
- No existing code modified; all changes are append-only
