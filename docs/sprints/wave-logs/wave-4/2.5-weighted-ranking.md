# Wave 4 Log: Story 2.5 -- Weighted Vendor Ranking

## Status: COMPLETE

## Changes Made

### 1. `app/scoring/engine.py` (APPENDED)
- Added `compute_vendor_rankings()` function
- Computes priority-weighted vendor scores normalized to 0-100 scale
- Uses `PRIORITY_WEIGHTS` from `app/config.py` for priority-based weighting
- Formula: `weighted_sum += score * weight * confidence` per requirement
- Normalization: `(weighted_sum / max_possible) * 100`
- Tiebreaker: alphabetical vendor name ordering
- Returns `list[VendorRanking]` with rank assigned by sorted position

### 2. `app/graph/nodes.py` (APPENDED)
- Added `compute_rankings()` async LangGraph node
- Reads `scores` and `requirements` from state
- Delegates to `compute_vendor_rankings()` engine function
- Emits progress at 85% with "Rankings computed" message
- Returns `{"rankings": rankings}` partial state update

### 3. `tests/test_scoring.py` (APPENDED)
- `test_full_scores_rank_first`: Vendor with perfect scores ranks #1
- `test_tiebreaker_alphabetical`: Equal scores sorted by vendor name
- `test_four_vendors_with_zeros`: All vendors returned even with zero scores
- `test_formula_manual_calculation`: Verifies exact formula output (61.5 for known inputs)
- `test_empty_scores_returns_empty`: Empty dict yields empty list

## Verification
- All 29 tests in `test_scoring.py` pass
- Lint passes on all modified files (`ruff check` clean)
- `app/scoring/engine.py` at 99% coverage
