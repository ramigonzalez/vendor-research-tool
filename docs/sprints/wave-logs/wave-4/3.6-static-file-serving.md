# Wave Log: Story 3.6 — Static File Serving & App Entry Point

## Status: Complete

## Changes Made

### 1. Modified `app/main.py`
- Added `lifespan` async context manager that calls `create_db_and_tables()` on startup
- Included API router from `app.api.router` (with try/except guard for parallel story safety)
- Mounted `/static` directory via `StaticFiles`
- Added root route (`/`) serving `static/index.html` via `FileResponse`
- Retained existing `/health` endpoint unchanged

### 2. Created `static/index.html`
- Minimal placeholder HTML page with title and body styling
- Serves as the frontend entry point until the real UI is built

### 3. Tests
- Existing `tests/test_health.py` passes without modification (FastAPI `TestClient` handles lifespan automatically)
- All 145 tests pass with no regressions

## Quality Gates
- `ruff check app/main.py` — passed
- `pyright app/main.py` — 0 errors, 0 warnings
- `pytest tests/ --no-cov` — 145 passed
